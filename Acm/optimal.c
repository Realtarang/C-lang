#include<stdio.h>
#include<math.h>

int main()
{
    int number, size;
    printf("enter the number : ");
    scanf("%d", &number);
    count(number) = size

    for(int i=0; i<size; i++)
    {
        int optimal;
        optimal = number
    }
}